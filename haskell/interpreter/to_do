- dodać przypisanie na element tablicy w gramatyce
 	--> poprawić shift/reduce conflict
 	ArrAss.    Stmt ::= Ident "{" Expr "}" "=" Expr ;
- dostęp do elementu tablicy w różnych środowiskach (?)
- return musi wychodzić z funkcji
- return w pętli for
- podawanie wyrażeń w indeksie tablicy w nawiasie

- poprawić prepArgs -> musi być w wywołaniu funkcji, bo rekurencja
					-> czy musi być przy wczytywaniu funkcji wtedy?

- dodać domyślną wartość przy inicjalizacji za krótką listą
- evalExpr(EApp) -> co zwraca jak funkcja typu void

TypeChecker
- czy funkcja przypadkiem nie dostaje pustego środowiska, bo nie widzi nic?
- zrobić catchowanie błędów, żeby budować sensowne komunikaty

kompilacja:
bnfc -m gramm.cf
make